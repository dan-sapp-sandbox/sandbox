export const revalidate = 0;

export default function MtgPage() {
  // use bookmarked api to get mtg data
  // use db for decks
  // use graphql and have toggle?
  // use clark or oath for single sign on
  return (
    <div className="max-w-5xl mx-auto mt-8">
    </div>
  );
}
